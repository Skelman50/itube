extends layouts/main

include mixins/videoBlock

block content
  .videos
    each video in videosList
      +videoBlock({
        id: video.id,
        title: video.title,
        views: video.views,
        fileUrl: video.fileUrl
      })